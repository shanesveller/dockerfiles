machine:
  services:
    - docker

dependencies:
  pre:
    - sudo service docker stop; sudo curl -L -o /usr/bin/docker 'http://s3-external-1.amazonaws.com/circle-downloads/docker-1.6.0-circleci'; sudo chmod 0755 /usr/bin/docker; sudo service docker start; true
  cache_directories:
    - "~/docker"

  override:
    - docker info
    - if [[ -e ~/docker/image.tar ]]; then docker load -i ~/docker/image.tar; fi
    - docker build -t shanesveller/elixir elixir/
    - mkdir -p ~/docker && docker save -o ~/docker/image.tar shanesveller/elixir

test:
  override:
    - docker run -it shanesveller/elixir iex -e ":erlang.halt"

